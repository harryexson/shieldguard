// The canonical roles, permissions and sidebar config now live in
// @shieldguard/shared (the platform's single source of truth). This file just
// re-exports them so nothing else in the office app needs to change.
export * from '@shieldguard/shared';
